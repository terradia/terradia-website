$(document).ready(function(){


    /*
    PERMET DE CHANGER LA COULEUR DE LA NAVBAR ON SCROLL IL FAUDRA INSTANCIER LA CLASS .scroll dans le CSS.
     */

    /*window.onscroll = () => {
        const header = document.querySelector('#header_nav');
        if(this.scrollY <= 10)
            header.className = '';
        else
            header.className = 'scroll';
    };*/

});